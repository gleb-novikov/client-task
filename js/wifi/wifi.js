/**
 * Функция проверки наличия доступа в Интернет.
 * @return {boolean} есть ли доступ в Интернет.
 */
function checkInternetConnection() {
    // ToDo: реализовать функцию, которая проверяет наличие доступа в Интернет
}
